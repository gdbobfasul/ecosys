// Автогенериран от build-mobile-apps.sh от publish/monetization.json — НЕ редактирай ръчно.
// Редактира се huawei/gl1-bloodstream/publish/monetization.json.
export const MONETIZATION = {
  "_comment": "МОНЕТИЗАЦИЯ на приложението — редактира се САМО ТУК. Билдът я вгражда в src/monetize.js. model: free|one_time|subscription|iap. released:true = ИМА релийз → без пробно заключване. trialLock: 4-дневно пробно заключване (само за тест преди издаване).",
  "model": "one_time",
  "released": true,
  "trialLock": {
    "enabled": false,
    "days": 4
  }
}
;
