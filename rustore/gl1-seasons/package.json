{
  "name": "gl1-seasons-rustore",
  "version": "1.0.0",
  "private": true,
  "description": "Seasongate — a warrior marches to the tower, switching the four seasons to reshape the world across 18 hand-tuned levels. Self-contained canvas-2D game + Capacitor (rustore build). Fully offline, no GMS/HMS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
