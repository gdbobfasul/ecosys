{
  "name": "gl1-candle-rustore",
  "version": "1.0.0",
  "private": true,
  "description": "Eternal Flame — guard one candle flame in the dark: parry wind, water, blades and stones from every side with the right defense. Self-contained canvas-2D reflex game + Capacitor (rustore build). Fully offline, no GMS/HMS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
