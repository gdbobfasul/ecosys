{
  "name": "business-faq-bot-rustore",
  "version": "1.0.0",
  "private": true,
  "description": "Бизнес FAQ-робот — наеми/активирай on-device robot, който отговаря на чести въпроси по правила (keyword match, БЕЗ платен LLM). Изцяло self-contained, без мрежа, без акаунти, без контакти, без проследяване (RUStore build).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:assets": "node tools/gen-assets.mjs",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "dependencies": {
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "vite": "^5.4.8"
  }
}
