# RUStore — Тестова среда / Закрито тестване

## 1. Браузър (основна тестова среда за разработка)
```
npm install
npm run dev      # отваря на http://localhost:5173
```
Играй директно в браузъра — това е средата за бърз тест на геймплея.

Управление (клавиатура):
- A / D или стрелки — движение
- W — скок
- SPACE — атака, SHIFT+SPACE — специален удар
- 1–5 — смяна на оръжие (само отключените)

На телефон: екранни бутони (D-pad ляво, ⚔/★/⤒ дясно).

## 2. Закрито тестване в RUStore (Closed testing)
RUStore Console -> Приложение -> "Тестирование" / "Закрытое тестирование":
1. Качи подписан APK/AAB (debug е достатъчен за вътрешен тест; за публикация — release подпис).
2. Добави имейли/акаунти на тестери в групата за закрито тестване.
3. Тестерите инсталират през линка за тестване в RUStore приложението.
4. Понеже играта няма билинг/мрежа, не е нужна sandbox среда за плащания.

## 3. Подписване
- Генерирай keystore (`keytool`), конфигурирай в `android/app/build.gradle` (release).
- Качи в RUStore Console (App signing).

## 4. Чеклист за публикация
Виж `CHECKLIST.md`.
