{
  "name": "titans-fight-rustore",
  "version": "1.0.0",
  "private": true,
  "description": "Godfist Arena - 2D fighting game (RUStore build). Self-contained Phaser 3 + Capacitor game. No GMS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android",
    "apk:debug": "echo 'See README.md - requires Android SDK/JDK (NOT installed on this machine)'"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "vite": "^5.4.8"
  }
}
