{
  "name": "gl1-shadow-rustore",
  "version": "1.0.0",
  "private": true,
  "description": "Shadowbound — lead only the shadow through 18 levels; every obstacle it fails wounds the real man above. Self-contained canvas-2D game + Capacitor (rustore build). Fully offline, no GMS/HMS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
