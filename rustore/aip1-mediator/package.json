{
  "name": "aip1-mediator-rustore",
  "version": "1.0.0",
  "private": true,
  "description": "Fair Mediator — describe a disagreement from both sides and get a calm, neutral mediation: fair summary, each side's needs, common ground and concrete next steps for non-violent communication. Self-contained web app + Capacitor (rustore build). Works offline with built-in guidance; optional user-supplied AI key/endpoint for a deeper AI-assisted mediation.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
