// Вградени мотивационни цитати (локални, БЕЗ мрежа).
// Връща детерминиран цитат за деня, за да е стабилен в рамките на същия ден.
const QUOTES = [
  'Малките навици днес строят големия теб утре.',
  'Започни сега — перфектният момент е мит.',
  'Дисциплината е свободата на бъдещия теб.',
  'Една стъпка дневно прави сто стъпки за три месеца.',
  'Не чакай мотивация — създай рутина.',
  'Това, което правиш всеки ден, значи повече от това, което правиш веднъж.',
  'Спокойният ум взема по-добри решения.',
  'Грижата за себе си не е лукс, а основа.',
  'Прогресът е по-важен от съвършенството.',
  'Утрото задава тона на целия ден.',
  'Малко по-добре от вчера е достатъчно.',
  'Фокусирай се върху следващото действие, не върху целия път.',
  'Почивката също е част от продуктивността.',
  'Постоянството побеждава таланта, когато талантът не е постоянен.',
  'Днес е добър ден да бъдеш малко по-добър.'
];

export function quoteForDay(date = new Date()) {
  // Индекс по ден от годината → стабилен за деня.
  const start = new Date(date.getFullYear(), 0, 0);
  const diff = date - start;
  const dayOfYear = Math.floor(diff / 86400000);
  return QUOTES[dayOfYear % QUOTES.length];
}

export function randomQuote() {
  return QUOTES[Math.floor(Math.random() * QUOTES.length)];
}

export { QUOTES };
