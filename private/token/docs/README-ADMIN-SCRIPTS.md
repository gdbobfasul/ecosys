<!-- Version: 1.0056 -->
# 🚀 KCY1 Token - Admin Scripts Documentation

## 📁 Файлова структура

```
scripts/
├── admin-config.js                  # 🔧 Конфигурация (РЕДАКТИРАЙ ТУК АДРЕСИТЕ!)
├── 01-check-status.js               # 📊 Проверка на статус
├── 02-pause-control.js              # ⏸️  Pause контрол
├── 03-blacklist-management.js       # 🚫 Blacklist управление
├── 04-exempt-slots-management.js    # 🔧 Exempt Slots управление
├── 05-liquidity-management.js       # 💧 Liquidity Pairs управление
├── 06-mint-operations.js            # 🌱 Mint операции
└── 07-token-operations.js           # 🔥 Token операции (burn, withdraw)
```

---

## ⚙️ SETUP (ВАЖНО - ПРОЧЕТИ ПЪРВО!)

### 1️⃣ Редактирай `admin-config.js`

**Отвори файла и промени:**

```javascript
module.exports = {
    // ОСНОВНИ АДРЕСИ - ПРОМЕНИ ТУК!
    TOKEN_ADDRESS: "0xadresnatokena",  // ← Адресът на deployed token
    ADMIN_ADDRESS: "0xE1B06c3886B08A5642D9eaD69e95a133e4cF39B9",  // ← Твоят admin адрес
    
    // Други адреси остават както са
    ...
};
```

### 2️⃣ Hardhat конфигурация

**Файл: `hardhat.config.js`**

Добави мрежата и настрой Ledger:

```javascript
require("@nomicfoundation/hardhat-toolbox");

module.exports = {
  solidity: "0.8.20",
  networks: {
    bscMainnet: {
      url: "https://bsc-dataseed.binance.org/",
      chainId: 56,
      accounts: ["твоя_private_key"]  // ЗА ТЕСТОВЕ САМО!
      // ИЛИ използвай Ledger с hardhat-ledger plugin
    },
    bscTestnet: {
      url: "https://data-seed-prebsc-1-s1.binance.org:8545/",
      chainId: 97,
      accounts: ["твоя_testnet_private_key"]
    }
  }
};
```

---

## 📚 РЪКОВОДСТВО ЗА ВСЕКИ СКРИПТ

### 📊 Скрипт 1: Проверка на статус

**Файл:** `01-check-status.js`

**Цел:** View функции - проверка на информация без промени

**Изпълнение:**
```bash
npx hardhat run scripts/01-check-status.js --network bscMainnet
```

**Какво показва:**
- ✅ Основна информация (име, символ, total supply)
- ✅ Owner и всички 10 exempt slots
- ✅ Твоята позиция (баланс, admin права)
- ✅ Pause & Lock статус
- ✅ DEX информация
- ✅ Mint proposals
- ✅ Cooldown времена

**Кога да го ползваш:**
- Преди всяка промяна (провери статус)
- След промяна (потвърди резултат)
- Когато имаш грешка (провери права)

---

### ⏸️ Скрипт 2: Pause контрол

**Файл:** `02-pause-control.js`

**Цел:** Пауза на trading за 48 часа

**Настройки:**
```javascript
const ACTION = 1; // ПРОМЕНИ ТУК
// 1 = Пауза на trading
// 2 = Само преглед
```

**Изпълнение:**
```bash
npx hardhat run scripts/02-pause-control.js --network bscMainnet
```

**Ефект:**
- ⏸️  Normal users НЕ могат да трансферират
- ✅ Exempt адресите МОГАТ да трансферират
- ⏰ Автоматично се отпаузва след 48h

**Кога да го ползваш:**
- Откриеш проблем със сигурността
- Искаш временно да спреш trading
- Emergency ситуация

---

### 🚫 Скрипт 3: Blacklist управление

**Файл:** `03-blacklist-management.js`

**Цел:** Добавяне/проверка на blacklist адреси

**Настройки:**
```javascript
const SCENARIO = 1; // ПРОМЕНИ ТУК
// 1 = Добавяне на ЕДИН адрес
// 2 = Добавяне на МНОГО адреси (batch)
// 3 = Проверка дали адрес е blacklisted
// 4 = Само преглед

// Адреси за blacklist:
const SINGLE_ADDRESS = "0x1234..."; // ПРОМЕНИ ТУК
const BATCH_ADDRESSES = ["0x111...", "0x222..."]; // ПРОМЕНИ ТУК
const CHECK_ADDRESS = "0x1234..."; // ПРОМЕНИ ТУК
```

**Изпълнение:**
```bash
npx hardhat run scripts/03-blacklist-management.js --network bscMainnet
```

**Ефект:**
- 🚫 Адресът НЕ може да transfer-ва
- 🚫 Адресът НЕ може да receive-ва
- ⚠️  САМО Multi-Sig може да премахне!

**Кога да го ползваш:**
- Откриеш подозрителна активност
- Scam или злоупотреба
- Нужда от блокиране на адрес

---

### 🔧 Скрипт 4: Exempt Slots управление

**Файл:** `04-exempt-slots-management.js`

**Цел:** Добавяне/промяна на admin адреси (exempt slots)

**Настройки:**
```javascript
const SCENARIO = 1; // ПРОМЕНИ ТУК
// 1 = Добавяне на НОВ Ledger (slots 6-10)
// 2 = Замяна на адрес в slot
// 3 = Заключване ЗАВИНАГИ
// 4 = Само преглед

// Настройки за сценарий 1:
const TARGET_SLOT = 6; // ПРОМЕНИ ТУК (6-10)
const NEW_ADDRESS = "0x4444..."; // ПРОМЕНИ ТУК
```

**Изпълнение:**
```bash
npx hardhat run scripts/04-exempt-slots-management.js --network bscMainnet
```

**Ефект:**
- ✅ Новият адрес получава admin права
- ⏸️  Trading се паузва за 48h АВТОМАТИЧНО!
- ⏰ Admin cooldown 48h АВТОМАТИЧНО!

**Важни правила:**
- ❌ Slot 1 НЕ може да се променя (owner)
- ⚠️  Slots 2-5 САМО Multi-Sig може да променя
- ✅ Slots 6-10 всеки Admin може да променя

**Кога да го ползваш:**
- Искаш да добавиш нов Ledger като admin
- Загубил си един Ledger и искаш да го замениш
- Финализиране на admin setup преди lock

---

### 💧 Скрипт 5: Liquidity управление

**Файл:** `05-liquidity-management.js`

**Цел:** Добавяне/управление на liquidity pairs

**Настройки:**
```javascript
const SCENARIO = 1; // ПРОМЕНИ ТУК
// 1 = Намиране на текущ pair (автоматично)
// 2 = Добавяне на НОВ pair
// 3 = Batch добавяне
// 4 = Премахване на pair
// 5 = Заключване ЗАВИНАГИ
// 6 = Само преглед

// Адреси:
const PAIR_ADDRESS = "0x7777..."; // ПРОМЕНИ ТУК
const BATCH_PAIRS = ["0x888...", "0x999..."]; // ПРОМЕНИ ТУК
```

**Изпълнение:**
```bash
npx hardhat run scripts/05-liquidity-management.js --network bscMainnet
```

**Workflow:**
1. Отиди на PancakeSwap и добави liquidity
2. Пусни SCENARIO = 1 (намира pair автоматично)
3. Пусни SCENARIO = 2 (добави pair като exempt)

**Ефект:**
- 💧 Pair става exempt (no fees, no limits)
- ⏰ Admin cooldown 48h

**Кога да го ползваш:**
- След добавяне на liquidity в PancakeSwap
- Искаш да добавиш нов trading pair
- Финализиране преди lock

---

### 🌱 Скрипт 6: Mint операции

**Файл:** `06-mint-operations.js`

**Цел:** Създаване на нови токени (mint)

**Настройки:**
```javascript
const SCENARIO = 1; // ПРОМЕНИ ТУК
// 1 = Предлагане на НОВ mint
// 2 = Изпълнение на proposal
// 3 = Проверка на proposals
// 4 = Само преглед

// Настройки:
const MINT_AMOUNT = ethers.parseEther("1000000"); // 1M токени - ПРОМЕНИ ТУК
const PROPOSAL_ID = 0; // За изпълнение - ПРОМЕНИ ТУК
```

**Изпълнение:**
```bash
# Стъпка 1: Предложи mint
npx hardhat run scripts/06-mint-operations.js --network bscMainnet

# Стъпка 2: Изчакай 1 ден (timelock)

# Стъпка 3: Изпълни mint (промени SCENARIO = 2)
npx hardhat run scripts/06-mint-operations.js --network bscMainnet
```

**Правила:**
- 📊 Max mint: 5,000,000 токена на proposal
- ⏰ Timelock: 1 ден преди execute
- 🔒 Cooldown: 1 седмица между mints

**Кога да го ползваш:**
- Нужда от нови токени
- Expansion на total supply
- Внимавай - УВЕЛИЧАВА total supply!

---

### 🔥 Скрипт 7: Token операции

**Файл:** `07-token-operations.js`

**Цел:** Burn, withdraw и emergency операции

**Настройки:**
```javascript
const SCENARIO = 1; // ПРОМЕНИ ТУК
// 1 = Burn токени (от твоя баланс)
// 2 = Withdraw токени от контракта
// 3 = Withdraw BNB от контракта
// 4 = Rescue объркани токени
// 5 = Само преглед

// Настройки:
const BURN_AMOUNT = ethers.parseEther("100000"); // ПРОМЕНИ ТУК
const WITHDRAW_AMOUNT = ethers.parseEther("50000"); // ПРОМЕНИ ТУК
const RESCUE_TOKEN_ADDRESS = "0xAnother..."; // ПРОМЕНИ ТУК
```

**Изпълнение:**
```bash
npx hardhat run scripts/07-token-operations.js --network bscMainnet
```

**Функции:**

**1. Burn (SCENARIO = 1)**
- 🔥 Изгаря токени от ТВОЯ баланс
- 📉 Total Supply намалява
- ❌ НЕОБРАТИМО!

**2. Withdraw от contract (SCENARIO = 2)**
- 📤 Изтегля KCY1 токени от контракта
- 📊 Total Supply НЕ се променя
- ✅ Преместват се токени

**3. Withdraw BNB (SCENARIO = 3)**
- 💎 Изтегля BNB от контракта
- 🚨 За emergency

**4. Rescue токени (SCENARIO = 4)**
- 🚨 Спасява объркани токени
- ❌ НЕ работи за KCY1!
- ✅ За други токени изпратени по грешка

**Кога да го ползваш:**
- Burn: Искаш да намалиш supply
- Withdraw: Нужда от токени от контракта
- Rescue: Някой е изпратил токени по грешка

---

## 🎯 ТИПИЧНИ СЦЕНАРИИ

### 🆕 Scenario 1: Добавяне на нов Ledger адрес като admin

```bash
# 1. Провери текущ статус
npx hardhat run scripts/01-check-status.js --network bscMainnet

# 2. Редактирай 04-exempt-slots-management.js:
#    SCENARIO = 1
#    TARGET_SLOT = 6 (или 7-10)
#    NEW_ADDRESS = "твоят_нов_ledger_адрес"

# 3. Изпълни
npx hardhat run scripts/04-exempt-slots-management.js --network bscMainnet

# 4. ⚠️  Trading е паузнат за 48h автоматично!
# 5. ⏰ Admin cooldown 48h автоматично!
```

---

### 🚨 Scenario 2: Emergency - спри trading

```bash
# 1. Редактирай 02-pause-control.js:
#    ACTION = 1

# 2. Изпълни
npx hardhat run scripts/02-pause-control.js --network bscMainnet

# 3. ✅ Trading е паузнат за 48h
```

---

### 💧 Scenario 3: Добавяне на liquidity pair

```bash
# 1. Отиди на PancakeSwap и добави liquidity

# 2. Намери pair адрес автоматично:
#    Редактирай 05-liquidity-management.js: SCENARIO = 1
npx hardhat run scripts/05-liquidity-management.js --network bscMainnet

# 3. Копирай pair адреса от output-а

# 4. Добави pair като exempt:
#    Редактирай 05-liquidity-management.js:
#    SCENARIO = 2
#    PAIR_ADDRESS = "копираният_адрес"
npx hardhat run scripts/05-liquidity-management.js --network bscMainnet

# 5. ✅ Pair е готов!
```

---

### 🌱 Scenario 4: Mint на нови токени

```bash
# 1. Редактирай 06-mint-operations.js:
#    SCENARIO = 1
#    MINT_AMOUNT = ethers.parseEther("1000000") # 1M токени

# 2. Предложи mint
npx hardhat run scripts/06-mint-operations.js --network bscMainnet

# 3. ⏰ ИЗЧАКАЙ 1 ДЕН (timelock)

# 4. Редактирай 06-mint-operations.js:
#    SCENARIO = 2
#    PROPOSAL_ID = 0 (от стъпка 2)

# 5. Изпълни mint
npx hardhat run scripts/06-mint-operations.js --network bscMainnet

# 6. ✅ 1M токени са mint-нати!
# 7. ⏰ Mint cooldown 1 седмица
```

---

### 🚫 Scenario 5: Blacklist на подозрителен адрес

```bash
# 1. Редактирай 03-blacklist-management.js:
#    SCENARIO = 1
#    SINGLE_ADDRESS = "подозрителният_адрес"

# 2. Изпълни
npx hardhat run scripts/03-blacklist-management.js --network bscMainnet

# 3. ✅ Адресът е блокиран!
# 4. ⚠️  САМО Multi-Sig може да го премахне!
```

---

## ⚠️ ВАЖНИ БЕЛЕЖКИ

### 🔴 Автоматични паузи и cooldowns

Някои функции АВТОМАТИЧНО активират пауза и cooldown:

| Функция | Pause | Cooldown |
|---------|-------|----------|
| updateExemptSlot() | 48h | 48h |
| pause() | 48h | - |
| updateDEXAddresses() | - | 48h |
| setLiquidityPair() | - | 48h |

**Важно:**
- ⏸️  Pause = Normal users НЕ могат да трансферират
- ⏰ Cooldown = НЕ можеш да променяш критични настройки

---

### 🔒 Lock операции

Някои lock операции са **НЕОБРАТИМИ** без Multi-Sig:

| Функция | Необратимо? | Unlock |
|---------|------------|--------|
| lockExemptSlotsForever() | ✅ ДА | unlockExemptSlots() (Multi-Sig) |
| lockDEXAddresses() | ✅ ДА | unlockDEXAddresses() (Multi-Sig) |
| lockLiquidityPairsForever() | ✅ ДА | unlockLiquidityPairs() (Multi-Sig) |

**Преди да lock-неш:**
- ✅ Провери всички адреси 2-3 пъти
- ✅ Запази списък offline
- ✅ Убеди се че всичко работи правилно

---

### 🎯 Slot правила

| Slot | Може да се променя от | Забележка |
|------|----------------------|-----------|
| Slot 1 | ❌ НИКОЙ | Owner - permanent |
| Slots 2-5 | Multi-Sig САМО | Критични slots |
| Slots 6-10 | Admin | Нормално управление |

---

## 🆘 Troubleshooting

### Грешка: "Not admin"
```
✅ Провери дали адресът е в exempt slots:
   npx hardhat run scripts/01-check-status.js --network bscMainnet
```

### Грешка: "Slots locked"
```
⚠️  Exempt slots са заключени!
💡 Само Multi-Sig може да unlock-не
   Функция: unlockExemptSlots()
```

### Грешка: "Admin cooldown"
```
⏰ Изчакай cooldown да изтече
📊 Провери кога изтича:
   npx hardhat run scripts/01-check-status.js --network bscMainnet
```

### Грешка: "Paused"
```
⏸️  Trading е паузнато
⏰ Изчакай 48h или използвай Multi-Sig за по-ранно отпаузване
```

---

## 📞 За помощ

1. Винаги започвай с `01-check-status.js` за проверка
2. Четете коментарите в скриптовете
3. Тествай ВИНАГИ на Testnet първо!
4. Запазвай backup на важни адреси

---

**Версия:** 1.0  
**Дата:** 09.02.2026  
**Admin адрес:** 0xE1B06c3886B08A5642D9eaD69e95a133e4cF39B9  
**Token адрес:** 0xadresnatokena

---

## 🎉 Готов си!

Всички скриптове са готови за използване. Започни с:

```bash
npx hardhat run scripts/01-check-status.js --network bscMainnet
```

Успех! 🚀
