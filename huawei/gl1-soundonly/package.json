{
  "name": "gl1-soundonly-huawei",
  "version": "1.0.0",
  "private": true,
  "description": "Echo Duel — hunt an enemy that gives itself away only by sound, strike the centre of the ping and dodge the counter across 100 levels. Self-contained canvas-2D + WebAudio game + Capacitor (huawei build). Fully offline, no GMS/HMS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
