// Автогенериран от build-mobile-apps.sh от publish/monetization.json — НЕ редактирай ръчно.
// Редактира се huawei/market-pulse/publish/monetization.json.
export const MONETIZATION = {
  "model": "one_time",
  "released": true,
  "trialLock": { "enabled": false, "days": 4 }
}
;
