{
  "name": "aip1-symptombridge-huawei",
  "version": "1.0.0",
  "private": true,
  "description": "Symptom Bridge — organise your complaint into a clear, structured summary for your doctor, in 15 languages. Optional user-supplied AI to tidy free text; works offline without it. Capacitor (huawei build). Not a medical device, does not diagnose.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
