{
  "name": "gl1-underworlds-huawei",
  "version": "1.0.0",
  "private": true,
  "description": "Core Descent — dig down through soil, rock, ore, crystal and magma to reach the glowing core, picking the right tool for each obstacle. Self-contained canvas-2D mining puzzle + Capacitor (huawei build). Fully offline, no GMS/HMS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
