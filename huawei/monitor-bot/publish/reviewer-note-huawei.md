# Reviewer note — Pupikes Site Monitor (Huawei)

## For reviewer
Addressed both points from the latest review:

(1.20 — app icon) The store icon has been re-uploaded and is now identical for ALL listing languages (incl. Traditional Chinese HK/TW and English US) and matches the launcher icon shown on the device after install (original Pupikes icon, label "pupikes").

(4.1 — richer content) The app is not a single static screen: it lets the user create UNLIMITED independent monitors, each watching a chosen RSS/Atom feed OR any public JSON API; per-monitor keyword matching; scheduled background checks (15 min / hourly / daily); change-diffing that detects new entries; local notifications on match; a manage/list view; add/edit/delete monitors; full 15-language interface; feedback tool. Everything runs on-device with no account. The full feature list is reflected in the updated description.
