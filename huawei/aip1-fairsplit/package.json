{
  "name": "aip1-fairsplit-huawei",
  "version": "1.0.0",
  "private": true,
  "description": "Fair Split — fair division assistant for expenses, inheritance items and chores. Self-contained web app + optional user-supplied AI (Anthropic/proxy) + Capacitor (huawei build). Works offline without an AI key.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
