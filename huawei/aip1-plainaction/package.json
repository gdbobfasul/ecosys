{
  "name": "aip1-plainaction-huawei",
  "version": "1.0.0",
  "private": true,
  "description": "Plain Action — paste any document, message, contract or notice and get a plain-language summary plus a clear checklist of what you actually need to do, with deadlines and drafted replies. Self-contained web app + Capacitor (huawei build). Works offline; optional user-supplied AI key/endpoint for deeper analysis.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "vite": "^5.4.8"
  }
}
