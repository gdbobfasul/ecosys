# ✅ v1.0066 - Node.js 18 Compatibility Fix

**Version:** 1.0066  
**Date:** February 16, 2026

---

## ❌ ПРОБЛЕМЪТ

```
npm warn EBADENGINE package: '@nomicfoundation/edr@0.12.0-next.23'
npm warn EBADENGINE required: { node: '>= 20' }
npm warn EBADENGINE current: { node: 'v18.20.8' }
```

**Имаш Node.js v18, но hardhat toolbox v5 изисква Node.js >= 20!**

Hardhat НЕ се е инсталирал → `npx hardhat` не го намира!

---

## ✅ РЕШЕНИЕ - 2 ВАРИАНТА

### ВАРИАНТ 1: Upgrade Node.js (ПРЕПОРЪЧВАМ)

```powershell
# 1. Изтегли Node.js 20 LTS:
# https://nodejs.org/en/download/

# 2. Инсталирай

# 3. Провери:
node --version
# Трябва: v20.x.x или v22.x.x

# 4. Reinstall:
rm -rf node_modules
npm install --legacy-peer-deps

# 5. Test:
npm test
```

**Защо препоръчвам:**
- ✅ Node 18 е вече EOL (End of Life)
- ✅ Node 20 LTS е активен
- ✅ По-добра performance
- ✅ Нови features

---

### ВАРИАНТ 2: Downgrade Hardhat (ВРЕМЕННО)

**Направих го за теб в v1.0066:**

```json
"devDependencies": {
  "@nomicfoundation/hardhat-toolbox": "^4.0.0",  // Беше ^5.0.0
  "hardhat": "^2.19.0"                            // Беше ^2.27.1
}
```

**Сега работи с Node 18:**

```powershell
# 1. Clean
rm -rf node_modules

# 2. Install
npm install --legacy-peer-deps

# 3. Test
npm test
```

**Недостатъци:**
- ❌ Стари версии (липсват features)
- ❌ Node 18 е EOL
- ❌ Скоро няма да се поддържа

---

## 🚀 ИНСТАЛАЦИЯ (Node 18)

```powershell
# 1. Clean старо
rm -rf node_modules

# 2. Install
$env:NODE_OPTIONS="--max-old-space-size=4096"
npm install --legacy-peer-deps

# 3. Test
npm test
```

**Сега БЕЗ engine warnings!**

---

## 📋 ПРОМЕНИ

### v1.0066:
- `@nomicfoundation/hardhat-toolbox`: `^5.0.0` → `^4.0.0`
- `hardhat`: `^2.27.1` → `^2.19.0`

**Версии съвместими с Node.js 18!**

---

## ⚠️ ВАЖНО

**Node 18 е End of Life (EOL) от April 2025!**

Препоръчвам upgrade на Node 20 LTS колкото по-скоро:

```bash
# Windows:
# https://nodejs.org/en/download/

# Linux/Mac:
# nvm install 20
# nvm use 20
```

---

## 🔍 ПРОВЕРКА

След `npm install --legacy-peer-deps`:

```bash
# Провери hardhat е инсталиран:
ls node_modules/.bin/hardhat
# Трябва да съществува

# Провери версия:
npx hardhat --version
# Трябва: 2.19.x

# Test:
npm test
# Трябва да работи
```

---

## 📊 Node.js Versions

| Version | Status | Поддръжка до |
|---------|--------|-------------|
| Node 18 | EOL | April 2025 |
| Node 20 | LTS | April 2026 |
| Node 22 | Current | - |

**Препоръка:** Node 20 LTS

---

**Status:** ✅ Работи с Node 18 (временно)  
**Препоръка:** Upgrade на Node 20
