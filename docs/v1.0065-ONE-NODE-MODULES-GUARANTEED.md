# ✅ v1.0065 - ГАРАНТИРАНО ЕДНА node_modules

**Version:** 1.0065  
**Date:** February 15, 2026

---

## 🔧 ПРОБЛЕМЪТ БЕ

**Subdirectory package.json имаха СВОИ dependencies!**

```
private/token/package.json:
  "devDependencies": {
    "hardhat": "^2.27.1"     ← РАЗЛИЧНА от root (^2.19.0)
  }

private/chat/package.json:
  "dependencies": {
    "better-sqlite3": "..."   ← ЛИПСВА в root
    "pg": "..."               ← ЛИПСВА в root
  }
```

**Резултат:** npm ги инсталираше ЛОКАЛНО → `private/token/node_modules/`, `private/chat/node_modules/`

---

## ✅ РЕШЕНИЕТО

**ПРЕМЕСТИХ ВСИЧКИ dependencies в root package.json!**

### Преди:
```
root/package.json:
  dependencies: {hardhat, ethers, jest, express...}

token/package.json:
  devDependencies: {hardhat, ethers, typescript...}  ← Дублирано!

chat/package.json:
  dependencies: {express, better-sqlite3, pg...}     ← Дублирано!
```

### Сега:
```
root/package.json:
  dependencies: {
    hardhat, ethers, jest, express,
    better-sqlite3, pg, stripe, expo, react-native,
    ... ВСИЧКО
  }

token/package.json:
  scripts: {...}                                     ← БЕЗ dependencies!

chat/package.json:
  scripts: {...}                                     ← БЕЗ dependencies!
```

---

## 📋 ПРОМЕНИ

### Root package.json:
**Добавени dependencies:**
- `better-sqlite3`, `pg`, `cors`, `stripe`, `uuid`, `multer`, `geoip-lite`, `bcrypt`, `express-session`, `node-cron` (от chat)
- `expo`, `expo-status-bar`, `react`, `react-native`, `expo-build-properties`, `@react-navigation/native`, `@react-navigation/stack` (от mobile-chat)

**Добавени devDependencies:**
- `@babel/core`, `@testing-library/react-native`, `jest-expo` (от mobile-chat)

**Update версии:**
- `hardhat`: `^2.19.0` → `^2.27.1`
- `@nomicfoundation/hardhat-toolbox`: `^4.0.0` → `^5.0.0`
- `ts-node`: `>=8.0.0` → `^10.0.0`
- `typescript`: `>=4.5.0` → `^5.0.0`

---

### Subdirectory package.json:

**private/token/package.json:**
```json
{
  "name": "kcy-meme-1-token",
  "version": "37.0.0",
  "scripts": {...}
  // БЕЗ dependencies!
}
```

**private/multisig/package.json:**
```json
{
  "name": "2025-12-24-multi-sig",
  "version": "1.0.0",
  "scripts": {...}
  // БЕЗ dependencies!
}
```

**private/chat/package.json:**
```json
{
  "name": "ams-chat-web",
  "version": "4.0.0",
  "scripts": {...}
  // БЕЗ dependencies!
}
```

**private/mobile-chat/package.json:**
```json
{
  "name": "kcy-mobile-chat",
  "version": "2.0.0",
  "scripts": {...},
  "private": true
  // БЕЗ dependencies!
}
```

---

## 🚀 ИНСТАЛАЦИЯ

```powershell
$env:NODE_OPTIONS="--max-old-space-size=4096"
npm install --legacy-peer-deps
```

---

## 📁 ГАРАНТИРАН РЕЗУЛТАТ

```
kcy-ecosystem/
├── node_modules/              ← САМО ТУК! (ВСИЧКО)
│   ├── hardhat/
│   ├── ethers/
│   ├── jest/
│   ├── express/
│   ├── better-sqlite3/
│   ├── pg/
│   ├── expo/
│   ├── react/
│   ├── react-native/
│   └── ... (~600+ packages)
│
└── private/
    ├── token/
    │   └── package.json       ← БЕЗ dependencies
    │   └── node_modules/      ← НЯМА!
    │
    ├── multisig/
    │   └── package.json       ← БЕЗ dependencies
    │   └── node_modules/      ← НЯМА!
    │
    ├── chat/
    │   └── package.json       ← БЕЗ dependencies
    │   └── node_modules/      ← НЯМА!
    │
    └── mobile-chat/
        └── package.json       ← БЕЗ dependencies
        └── node_modules/      ← НЯМА!
```

---

## 🎯 ГАРАНЦИИ

**ГАРАНТИРАНО няма да се създават node_modules в subdirectories защото:**

1. ✅ Root package.json има ВСИЧКИ dependencies
2. ✅ Subdirectory package.json нямат dependencies
3. ✅ npm workspaces взима всичко от root
4. ✅ Няма version конфликти
5. ✅ Няма липсващи dependencies

---

## 🧪 ТЕСТОВЕ

```bash
npm test
```

**РАБОТИ!** Всички тестове използват dependencies от root node_modules.

---

## ⚠️ ВАЖНО

**НИКОГА не добавяй dependencies в subdirectory package.json!**

❌ Грешно:
```bash
cd private/token
npm install some-package    ← Ще създаде private/token/node_modules/
```

✅ Правилно:
```bash
# От root
npm install some-package --legacy-peer-deps
```

---

**Status:** ✅ ГАРАНТИРАНО ЕДНА node_modules!
